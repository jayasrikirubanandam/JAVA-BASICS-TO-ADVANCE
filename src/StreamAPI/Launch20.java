package StreamAPI;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.function.Predicate;

public class Launch20 {

	public static void main(String[] args) {
		
	/*ArrayList<Integer>  personAge = new ArrayList<>(Arrays.asList(56,2,3,78,23));
	
	
	
			//System.out.println(personAge.stream().filter(i-> (i>18)).count());
	
	 int smallestEle = personAge.stream().sorted().findFirst().get();
	 
	 int largeEle = personAge.stream().sorted(Comparator.reverseOrder()).findFirst().get();
	  int secondLargest = personAge.stream().sorted(Comparator.reverseOrder()).skip(1).findFirst().get();
	  System.out.println(smallestEle + " "+largeEle + " " + secondLargest );

	  int check = personAge.stream()
	  		.sorted((a,b) -> Integer.compare(a, b))
	  		.findFirst().get();
	  
	  System.out.println(check);*/
	  
	  
	 /* List<String> sentences = Arrays.asList("I am learning Java", "The sky is blue","I love you");
	  
	  List<Integer> noOfWords = new ArrayList<Integer>();
	  
	  for(String s: sentences) {
		    String[] words = s.split(" ");
		    noOfWords.add(words.length);
	  }
	  for(int i : noOfWords) {
		  System.out.println(i);
	  }
	  
	  sentences.stream().map(sentence -> sentence.split("\\s+"))
	              .map(word -> word.length).forEach(count -> System.out.println(count));*/
		
		
		
	  
	}

}
